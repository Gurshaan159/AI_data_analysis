# Python Wrapper Stubs

This folder is reserved for future Python-based pipeline wrapper scripts.

- Keep wrapper scripts deterministic and side-effect aware.
- Expose stable CLI contracts so Rust orchestration can call wrappers safely.
- Add one folder per pipeline family when implementation begins.
